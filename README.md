Face Detection Project
This repository showcases a face detection system implemented using OpenCV and the Haar Cascade classifier. The project demonstrates the use of pre-trained Haar Cascade XML files to efficiently detect faces in images and video streams.
